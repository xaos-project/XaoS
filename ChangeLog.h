o Line drawing functions - not very fast,
  but performs antialiasing etc..o Enabled forgotten optimizations for bpp ==
  1 o Better I / B / P frame decision
  algorithm (now seems to give quite good results)
     o Motion vector guessing ...
       o Fixed some bugs in antialiasing,
       vertical /
       horizontal line drawing and such o Some new options for rendering o
       Document how to generate seqences for MPEG encoding o Fixed command
       flags - now works commands as loadpos
