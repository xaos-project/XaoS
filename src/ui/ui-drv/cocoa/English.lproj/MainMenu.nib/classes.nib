{
    IBClasses = (
        {
            ACTIONS = {performMenuAction = id; }; 
            CLASS = AppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {view = FractalView; window = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {CLASS = FractalView; LANGUAGE = ObjC; SUPERCLASS = NSView; }
    ); 
    IBVersion = 1; 
}